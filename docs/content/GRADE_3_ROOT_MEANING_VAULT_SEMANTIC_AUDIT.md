# Grade 3 Root Meaning Vault Semantic Audit

## Audit result

Deterministic semantic audit issues remaining: zero. Human/Ultra morpheme review issues remaining: zero. Vocabulary substitutions: zero.

## Target-by-target morpheme review

| Target | Primary family | Defensible part clue | Context-confirmed meaning |
| --- | --- | --- | --- |
| thermometer | Greek root | `therm` heat + connector `o` + `meter` measure | an instrument that measures temperature |
| thermal | Greek root | `therm` heat + `al` relating to | relating to heat |
| polygon | Greek root | `poly` many + `gon` angle | a shape with many angles/sides |
| monorail | Greek root | `mono` one + English `rail` | a railway using one rail |
| astronaut | Greek root | `astro` star + `naut` traveler | a traveler in space |
| biology | Greek root | `bio` life + connector `o` + `logy` study | the study of living things |
| telegram | Greek root | `tele` far + `gram` written message | a message sent over a distance |
| portable | Latin root | `port` carry + `able` can be | able to be carried |
| import | Latin root | `im` into + `port` carry | bring or carry into a place |
| predict | Latin root | `pre` before + `dict` say | say what is likely before it happens |
| visible | Latin root | `vis` see + `ible` can be | able to be seen |
| audible | Latin root | `aud` hear + `ible` can be | able to be heard |
| inspect | Latin root | `in` into + `spect` look | look at closely |
| aqueduct | Latin root | surface `aque`, canonical `aqua` water + `duct` lead/carry | a structure that carries water |
| preview | English prefix/base | `pre` before + `view` look | look at before the main event |
| reread | English prefix/base | `re` again + `read` | read again |
| miscount | English prefix/base | `mis` wrongly + `count` | count incorrectly |
| unclear | English prefix/base | `un` not + `clear` | not clear |
| preheat | English prefix/base | `pre` before + `heat` | heat before using |
| disconnect | English prefix/base | `dis` apart + `connect` | separate a connection |
| nonfiction | English prefix/base | `non` not + `fiction` | writing that is not fiction |
| hopeful | English base/suffix | `hope` + `ful` having/full of | having hope |
| careless | English base/suffix | `care` + `less` without | without enough care |
| washable | English base/suffix | `wash` + `able` can be | able to be washed |
| kindness | English base/suffix | `kind` + `ness` state/quality | the quality of being kind |
| agreement | English base/suffix | `agree` + `ment` result/state | a shared decision or state of agreeing |
| refillable | English base/suffix | `re` again + `fill` + `able` can be | able to be filled again |
| readable | English base/suffix | `read` + `able` can be | able or easy to read |

## Boundary findings

- False roots: zero. Coincidental letter matches are never scored as morphemes.
- Opaque compositions: zero. Part clues point reliably to each contextual meaning.
- Origin mismatches: zero after the surface/canonical `aqueduct` correction.
- Context contradictions: zero. Each target has source-owned confirming evidence.
- Pronunciation confusion: zero. Meaning parts and spoken Word Help chunks are separate authored data.
- Root Reactor drift: zero. Scored work asks what parts suggest and how context confirms meaning.
- Suffix Shifter drift: zero. Part-of-speech transformation is not the primary construct.
- Meaning Maze drift: zero. Context-clue types, reference materials, relationships, multiple meanings, and figurative phrases remain unstarted.
- Prompt ambiguity, prompt leakage, duplicate visible choices, stale evidence, and unresolved ownership: zero.

All content remains DRAFT. This repository audit is not educator approval or learner-mastery evidence.
